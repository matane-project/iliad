import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'one.geteasy.iliad',
  appName: 'Iliad',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
